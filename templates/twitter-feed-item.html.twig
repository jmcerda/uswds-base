{% if avatar %}
<div class="avatar">
  <img src="{{ avatar.profile_image_url }}" alt="@{{ avatar.screen_name }}" />
{#  <span>{{ avatar.name }}</span>#}
{#  <a href="{{ avatar.url }}">@{{ avatar.screen_name }}</a>#}
</div>
{% endif %}
<div class="tweet">{{ text }}</div>
<a href="{{ avatar.url }}">@{{ avatar.screen_name }}</a> - <time class="timeago" datetime="{{ date.stamp }}">{{ date.clean }}</time>
