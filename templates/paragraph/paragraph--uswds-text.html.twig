{% block paragraph %}
  <div{{ attributes }}>
    {% block content %}

        {% if uswds_grid_class %}
        <div class="{{ uswds_grid_class }} usa-prose">
        {% endif %}

          {{ content }}

        {% if uswds_grid_class %}
        </div>
        {% endif %}

    {% endblock %}
  </div>
{% endblock paragraph %}
