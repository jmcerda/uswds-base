{% block paragraph %}
  {%
    set classes = [
    'usa-media-block',
    'tablet:grid-col',
  ]
  %}
  <div{{ attributes.addClass(classes) }}>
    {% block content %}
      <img class="usa-media-block__img"  src="{{ file_url(content.field_image['#items'].entity.uri.value) }}" alt="{{ content.field_image.alt }}">
      <div class="tmp">{{ content.field_image }}</div>
      <div class="usa-media-block__body">
        <h2 class="usa-graphic-list__heading">{{ paragraph.field_title.value }}</h2>
        {{ content|without('field_image', 'field_title') }}
      </div>
    {% endblock %}
  </div>
{% endblock paragraph %}


{#{% set blah = content.field_image.attributes.addClass('usa-media-block__img') %}#}
{#      {{ content.field_image }}#}
